- @primary_nav = :users

%h2 Users
%table.admin-table
  %tr
    %th Username
    %th Timezone
    %th Actions
  - @users.each do |user|
    %tr{:class => cycle('even', 'odd')}
      %td= user.username
      %td= user.timezone
      %td.actions #{link_to 'View', [:admin, user]}
