- @primary_nav = :users

%h2 Users
%table
  %tr
    %th Username
    %th Timezone
  - @users.each do |user|
    %tr{:class => cycle('even', 'odd')}
      %td= link_to user.username, [:admin, user]
      %td= user.timezone
